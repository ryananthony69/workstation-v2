<UserControl x:Class="WorkstationV2.Controls.ScriptRunner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="Input (Ctrl+Enter):" VerticalAlignment="Center"/>
            <Button x:Name="RunButton" Content="Run" Padding="10,4" Margin="10,0,0,0" Click="Run_Click" DockPanel.Dock="Right"/>
        </DockPanel>

        <TextBox x:Name="InputBox"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 KeyDown="InputBox_KeyDown"/>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,6,0,6" Opacity="0.8"/>

        <TextBox x:Name="OutputBox"
                 Grid.Row="3"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"/>
    </Grid>
</UserControl>